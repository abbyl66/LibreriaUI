import 'package:flutter/material.dart';

class Ajustes extends StatelessWidget {

  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text("Ajustes"),
          centerTitle: true,
          backgroundColor: Color(0xE7E7E7),
        ),
      );
}