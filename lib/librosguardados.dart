import 'package:flutter/material.dart';
import 'package:biblioteca_disenio/main.dart';

class Librosguardados extends StatelessWidget {

  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text("Libros guardados"),
          centerTitle: true,
          backgroundColor: Color(0xFFC2DBDF),
        ),
        body: ListView(
          children: <Widget>[
          ],
        ),
      );
}