import 'package:flutter/material.dart';

class Librosfav extends StatelessWidget {

  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text("Libros favoritos"),
          centerTitle: true,
          backgroundColor: Color(0xFFEAABBC),
        ),
      );
}